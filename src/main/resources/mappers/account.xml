<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huajie.educomponent.usercenter.mapper.AccountMapper">
    <select id="getAccountById" parameterType="string" resultType="com.huajie.educomponent.usercenter.entity.AccountEntity">
        select * from account_account where id=#{id}
    </select>
</mapper>

